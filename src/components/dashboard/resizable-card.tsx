"use client";

/* This file is no longer used — dashboard now uses react-grid-layout directly.
   Kept as a stub to avoid import errors during transition. */
export function ResizableCard({ children, className, defaultSpan, minSpan, maxSpan }: any) {
  return <div className={className}>{children}</div>;
}
